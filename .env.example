# Vercel Blob Storage Token (Required for production screenshot uploads)
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

# (Optional) Remote LLM Endpoint for cloud AI analysis fallback
VITE_REMOTE_LLM_ENDPOINT="https://api.your-inference-endpoint.com/v1"
VITE_REMOTE_LLM_API_KEY="your-api-key"
